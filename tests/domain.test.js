import {test} from 'node:test';import assert from 'node:assert/strict';import {cents,metrics,filterRows,validDate,previousPeriod,fingerprint} from '../src/domain.js';
test('importi italiani e centesimi',()=>{assert.equal(cents('1.234,56 €'),123456);assert.equal(cents('-0,29'),-29);assert.throws(()=>cents(''));});
test('trasferimenti e saldi non gonfiano le metriche',()=>{const m=metrics([{kind:'income',amount:200000,date:'2026-01-01'},{kind:'expense',amount:-50000,category:'Casa',date:'2026-01-02'},{kind:'refund',amount:10000,category:'Casa',date:'2026-01-03'},{kind:'transfer',amount:-100000,date:'2026-01-04'},{kind:'opening',amount:1000000,date:'2026-01-01'}]);assert.equal(m.spending,40000);assert.equal(m.saving,160000);assert.equal(m.rate,.8);});
test('filtri inclusivi e date reali',()=>{assert.equal(filterRows([{date:'2026-01-01'},{date:'2026-01-31'},{date:'2026-02-01'}],{from:'2026-01-01',to:'2026-01-31'}).length,2);assert.equal(validDate('2026-02-30'),false);assert.equal(validDate('2024-02-29'),true);});
test('confronto su stesso numero di giorni',()=>{assert.deepEqual(previousPeriod('2026-03-01','2026-03-10'),{from:'2026-02-19',to:'2026-02-28'});assert.equal(metrics([]).rate,null);});
test('duplicati non attraversano conti',()=>{const r={date:'2026-01-01',account:'A',person:'Peng',amount:-100,description:' Caffè '};assert.notEqual(fingerprint(r),fingerprint({...r,account:'B'}));});
